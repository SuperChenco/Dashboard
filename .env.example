# Public application configuration
PUBLIC_APP_URL=http://localhost:4321

# Supabase public client configuration.
# Use the project URL and publishable/anon key; never put service-role keys here.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only AI configuration (intentionally unused until Phase 4)
OPENAI_MODEL=
OPENAI_API_KEY=

# Server runtime label
APP_ENV=development
